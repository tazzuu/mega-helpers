# from ChatGPT ; thanks ChatGPT!
set ssl_starttls=yes
set ssl_force_tls=yes
set imap_user = "myemail@gmail.com"
set imap_pass = "app-password-goes-here"
set from = "your_email@gmail.com"
set realname = "Your Name"
set folder = "imaps://imap.gmail.com/"
set spoolfile = "+INBOX"
set postponed = "+[Gmail]/Drafts"
set header_cache = ~/.mutt/cache/headers
set message_cachedir = ~/.mutt/cache/bodies
set certificate_file = ~/.mutt/certificates
set move = no


# if you wanted to use SMTP the setting would look like this but we are NOT using it!
# set smtp_url = "smtp://your_email@gmail.com@smtp.gmail.com:587/"
# set smtp_pass = "your_password"



